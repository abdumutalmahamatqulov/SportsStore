@model SportsStore.Domain.Models.SendModel
@{
    ViewData["Title"] = "Check Verification Code";
}

<h1>@ViewData["Title"]</h1>

<form asp-controller="Auth" asp-action="CheckVerificationCode" method="post">
    <div class="form-group">
        <label asp-for="Email" type="hidden" class="col-md-2 control-label"></label>
        <div class="col-md-10">
            <input asp-for="Email" class="form-control"type="hidden"/>
             <span asp-validation-for="Email" class="text-danger"></span> 
        </div>
    </div>
    <div class="form-group">
        <label asp-for="Code" class="col-md-2 control-label"></label>
        <div class="col-md-2">
            <input asp-for="Code" class="form-control" />
            <span asp-validation-for="Code" class="text-danger"></span>
        </div>
    </div>
    <input type="hidden" name="returnUrl" value="@(ViewBag.ReturnUrl??"/Login")" />
    <div>
        <button type="submit" class="btn btn-success">Check Verification Code</button>
        <a asp-controller="Auth" asp-action="Login" asp-route-returnUrl="@ViewBag.ReturnUrl" class="btn btn-danger">Cancel</a>
    </div>
    <div asp-validation-summary="All" class="bg-outline-danger text-danger" />
</form>
